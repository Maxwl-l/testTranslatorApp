A simple translator app that uses the googletrans library.
Followed a tutorial by @Code with Josh on youtube, this code uses Python 3.12 since the googletrans library only worked on that specific version using Python's cgi module
